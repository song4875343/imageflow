[project]
name = "image-edit"
version = "0.1.0"
description = "图片编辑 + 空间设计（EROOM）桌面应用"
requires-python = ">=3.9"
dependencies = [
    "pywebview>=5.0",
    "Pillow>=9.0",
    "numpy>=1.21",
    "opencv-python>=4.6",
    "requests>=2.28",
    "openai>=1.0",
]

[tool.uv]
package = false
